### no 461

#### code

```python
class Solution:
    def hammingDistance(self, x, y):
        """
        :type x: int
        :type y: int
        :rtype: int
        """
        acc = 0
        t = x^y 
        while t:
            acc += t&0x01
            t>>=1
        return acc
```
```c
int hammingDistance(int x, int y) {
    int acc=0;
    int t = x^y;
    while(t){
        acc+=t&0x01;
        t>>=1;
    }
    return acc;
}
```